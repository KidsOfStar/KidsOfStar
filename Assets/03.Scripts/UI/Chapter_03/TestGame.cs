using UnityEngine;

public class TestGame : MonoBehaviour
{
    public GameObject Stop; // 테스트 프리팹
    // Start is called before the first frame update
    void Start()
    {
        // 시작할 때


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
